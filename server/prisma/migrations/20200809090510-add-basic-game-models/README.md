# Migration `20200809090510-add-basic-game-models`

This migration has been generated by Brayden at 8/9/2020, 7:05:10 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "Game" (
"id" TEXT NOT NULL,
"createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("id"))

CREATE TABLE "Hand" (
"id" TEXT NOT NULL,
PRIMARY KEY ("id"))

CREATE TABLE "Move" (
"id" TEXT NOT NULL,
"playerId" TEXT NOT NULL,
"gameId" TEXT NOT NULL,
PRIMARY KEY ("id"),
FOREIGN KEY ("playerId") REFERENCES "Player"("id") ON DELETE CASCADE ON UPDATE CASCADE,

FOREIGN KEY ("gameId") REFERENCES "Game"("id") ON DELETE CASCADE ON UPDATE CASCADE
)

CREATE TABLE "Player" (
"id" TEXT NOT NULL,
"createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
"handId" TEXT NOT NULL,
PRIMARY KEY ("id"),
FOREIGN KEY ("handId") REFERENCES "Hand"("id") ON DELETE CASCADE ON UPDATE CASCADE
)

CREATE TABLE "_CardToHand" (
"A" INTEGER NOT NULL,
"B" TEXT NOT NULL,
FOREIGN KEY ("A") REFERENCES "Card"("id") ON DELETE CASCADE ON UPDATE CASCADE,

FOREIGN KEY ("B") REFERENCES "Hand"("id") ON DELETE CASCADE ON UPDATE CASCADE
)

CREATE TABLE "_CardToMove" (
"A" INTEGER NOT NULL,
"B" TEXT NOT NULL,
FOREIGN KEY ("A") REFERENCES "Card"("id") ON DELETE CASCADE ON UPDATE CASCADE,

FOREIGN KEY ("B") REFERENCES "Move"("id") ON DELETE CASCADE ON UPDATE CASCADE
)

CREATE TABLE "_GameToPlayer" (
"A" TEXT NOT NULL,
"B" TEXT NOT NULL,
FOREIGN KEY ("A") REFERENCES "Game"("id") ON DELETE CASCADE ON UPDATE CASCADE,

FOREIGN KEY ("B") REFERENCES "Player"("id") ON DELETE CASCADE ON UPDATE CASCADE
)

CREATE UNIQUE INDEX "_CardToHand_AB_unique" ON "_CardToHand"("A","B")

CREATE  INDEX "_CardToHand_B_index" ON "_CardToHand"("B")

CREATE UNIQUE INDEX "_CardToMove_AB_unique" ON "_CardToMove"("A","B")

CREATE  INDEX "_CardToMove_B_index" ON "_CardToMove"("B")

CREATE UNIQUE INDEX "_GameToPlayer_AB_unique" ON "_GameToPlayer"("A","B")

CREATE  INDEX "_GameToPlayer_B_index" ON "_GameToPlayer"("B")

PRAGMA foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200809004302-init..20200809090510-add-basic-game-models
--- datamodel.dml
+++ datamodel.dml
@@ -1,19 +1,52 @@
 datasource db {
     provider = "sqlite" 
-    url = "***"
+    url = "***"
 }
 generator client {
     provider = "prisma-client-js"
 }
 model Card {
-    id          Int      @id @default(autoincrement())
-    suit        Suit
-    value       Value
+    id          Int     @id @default(autoincrement())
+    suit        Suit    @relation(fields:  [suitValue], references: [value])
+    suitValue   Int
+    value       Value   @relation(fields:  [valueValue], references: [value])
+    valueValue  Int
+    hands       Hand[]  @relation(references: [id])
+    moves       Move[]  @relation(references: [id])
 }
+model Game {
+    id              String      @id @default(uuid())
+    createdAt       DateTime    @default(now())
+    players         Player[]    @relation(references: [id])
+    pile            Move[]      
+}
+
+model Hand {
+    id  String @id @default(uuid())
+    cards Card[] @relation(references: [id])
+}
+
+model Move {
+    id          String  @id @default(uuid())
+    player      Player  @relation(fields: [playerId], references: [id])
+    playerId    String 
+    cards       Card[]  @relation(references: [id])
+    game        Game    @relation(fields: [gameId], references: [id])
+    gameId      String
+}
+
+model Player {
+    id          String      @id @default(uuid())
+    createdAt   DateTime    @default(now())
+    hand        Hand        @relation(fields: [handId], references: [id])
+    handId      String
+    games       Game[]      @relation(references: [id])
+}
+
 model Suit {
     value       Int     @id @default(autoincrement())
     name        String
 }
```


