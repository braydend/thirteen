# Migration `20200810104104-make-player-hands-optional`

This migration has been generated by Brayden at 8/10/2020, 8:41:04 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "new_Player" (
"id" TEXT NOT NULL,
"createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
"handId" TEXT ,
PRIMARY KEY ("id"),
FOREIGN KEY ("handId") REFERENCES "Hand"("id") ON DELETE SET NULL ON UPDATE CASCADE
)

INSERT INTO "new_Player" ("id", "createdAt", "handId") SELECT "id", "createdAt", "handId" FROM "Player"

PRAGMA foreign_keys=off;
DROP TABLE "Player";;
PRAGMA foreign_keys=on

ALTER TABLE "new_Player" RENAME TO "Player";

CREATE TABLE "new_Suit" (
"value" INTEGER NOT NULL,
"name" TEXT NOT NULL,
PRIMARY KEY ("value"))

INSERT INTO "new_Suit" ("value", "name") SELECT "value", "name" FROM "Suit"

PRAGMA foreign_keys=off;
DROP TABLE "Suit";;
PRAGMA foreign_keys=on

ALTER TABLE "new_Suit" RENAME TO "Suit";

CREATE UNIQUE INDEX "Suit.name_unique" ON "Suit"("name")

CREATE TABLE "new_Value" (
"value" INTEGER NOT NULL,
"name" TEXT NOT NULL,
PRIMARY KEY ("value"))

INSERT INTO "new_Value" ("value", "name") SELECT "value", "name" FROM "Value"

PRAGMA foreign_keys=off;
DROP TABLE "Value";;
PRAGMA foreign_keys=on

ALTER TABLE "new_Value" RENAME TO "Value";

CREATE UNIQUE INDEX "Value.name_unique" ON "Value"("name")

PRAGMA foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200810100323-hardcode-suit-and-value-values..20200810104104-make-player-hands-optional
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
     provider = "sqlite" 
-    url = "***"
+    url = "***"
 }
 generator client {
     provider = "prisma-client-js"
@@ -41,10 +41,10 @@
 model Player {
     id          String      @id @default(uuid())
     createdAt   DateTime    @default(now())
-    hand        Hand        @relation(fields: [handId], references: [id])
-    handId      String
+    hand        Hand?        @relation(fields: [handId], references: [id])
+    handId      String?
     games       Game[]      @relation(references: [id])
 }
 model Suit {
```


