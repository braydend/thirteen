# Migration `20200809113826-card-suit-and-value-unique-constraint`

This migration has been generated by Brayden at 8/9/2020, 9:38:26 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "Card" (
"id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
"suitValue" INTEGER NOT NULL,
"valueValue" INTEGER NOT NULL,
FOREIGN KEY ("suitValue") REFERENCES "Suit"("value") ON DELETE CASCADE ON UPDATE CASCADE,

FOREIGN KEY ("valueValue") REFERENCES "Value"("value") ON DELETE CASCADE ON UPDATE CASCADE
)

CREATE TABLE "Game" (
"id" TEXT NOT NULL,
"createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("id"))

CREATE TABLE "Hand" (
"id" TEXT NOT NULL,
PRIMARY KEY ("id"))

CREATE TABLE "Move" (
"id" TEXT NOT NULL,
"playerId" TEXT NOT NULL,
"gameId" TEXT NOT NULL,
PRIMARY KEY ("id"),
FOREIGN KEY ("playerId") REFERENCES "Player"("id") ON DELETE CASCADE ON UPDATE CASCADE,

FOREIGN KEY ("gameId") REFERENCES "Game"("id") ON DELETE CASCADE ON UPDATE CASCADE
)

CREATE TABLE "Player" (
"id" TEXT NOT NULL,
"createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
"handId" TEXT NOT NULL,
PRIMARY KEY ("id"),
FOREIGN KEY ("handId") REFERENCES "Hand"("id") ON DELETE CASCADE ON UPDATE CASCADE
)

CREATE TABLE "Suit" (
"value" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
"name" TEXT NOT NULL)

CREATE TABLE "Value" (
"value" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
"name" TEXT NOT NULL)

CREATE TABLE "_CardToHand" (
"A" INTEGER NOT NULL,
"B" TEXT NOT NULL,
FOREIGN KEY ("A") REFERENCES "Card"("id") ON DELETE CASCADE ON UPDATE CASCADE,

FOREIGN KEY ("B") REFERENCES "Hand"("id") ON DELETE CASCADE ON UPDATE CASCADE
)

CREATE TABLE "_CardToMove" (
"A" INTEGER NOT NULL,
"B" TEXT NOT NULL,
FOREIGN KEY ("A") REFERENCES "Card"("id") ON DELETE CASCADE ON UPDATE CASCADE,

FOREIGN KEY ("B") REFERENCES "Move"("id") ON DELETE CASCADE ON UPDATE CASCADE
)

CREATE TABLE "_GameToPlayer" (
"A" TEXT NOT NULL,
"B" TEXT NOT NULL,
FOREIGN KEY ("A") REFERENCES "Game"("id") ON DELETE CASCADE ON UPDATE CASCADE,

FOREIGN KEY ("B") REFERENCES "Player"("id") ON DELETE CASCADE ON UPDATE CASCADE
)

CREATE UNIQUE INDEX "Card.suitValue_valueValue_unique" ON "Card"("suitValue","valueValue")

CREATE UNIQUE INDEX "Suit.name_unique" ON "Suit"("name")

CREATE UNIQUE INDEX "Value.name_unique" ON "Value"("name")

CREATE UNIQUE INDEX "_CardToHand_AB_unique" ON "_CardToHand"("A","B")

CREATE  INDEX "_CardToHand_B_index" ON "_CardToHand"("B")

CREATE UNIQUE INDEX "_CardToMove_AB_unique" ON "_CardToMove"("A","B")

CREATE  INDEX "_CardToMove_B_index" ON "_CardToMove"("B")

CREATE UNIQUE INDEX "_GameToPlayer_AB_unique" ON "_GameToPlayer"("A","B")

CREATE  INDEX "_GameToPlayer_B_index" ON "_GameToPlayer"("B")

PRAGMA foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200809090510-add-basic-game-models..20200809113826-card-suit-and-value-unique-constraint
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
     provider = "sqlite" 
-    url = "***"
+    url = "***"
 }
 generator client {
     provider = "prisma-client-js"
@@ -14,8 +14,9 @@
     value       Value   @relation(fields:  [valueValue], references: [value])
     valueValue  Int
     hands       Hand[]  @relation(references: [id])
     moves       Move[]  @relation(references: [id])
+    @@unique([suitValue, valueValue])
 }
 model Game {
     id              String      @id @default(uuid())
@@ -47,11 +48,11 @@
 }
 model Suit {
     value       Int     @id @default(autoincrement())
-    name        String
+    name        String  @unique
 }
 model Value {
     value       Int     @id @default(autoincrement())
-    name        String
+    name        String  @unique
 }
```


