# Migration `20200809004302-init`

This migration has been generated by Brayden at 8/9/2020, 10:43:02 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "Card" (
"id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
"suitValue" INTEGER NOT NULL,
"valueValue" INTEGER NOT NULL,
FOREIGN KEY ("suitValue") REFERENCES "Suit"("value") ON DELETE CASCADE ON UPDATE CASCADE,

FOREIGN KEY ("valueValue") REFERENCES "Value"("value") ON DELETE CASCADE ON UPDATE CASCADE
)

CREATE TABLE "Suit" (
"value" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
"name" TEXT NOT NULL)

CREATE TABLE "Value" (
"value" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
"name" TEXT NOT NULL)

PRAGMA foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200809004302-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,24 @@
+datasource db {
+    provider = "sqlite" 
+    url = "***"
+}
+
+generator client {
+    provider = "prisma-client-js"
+}
+
+model Card {
+    id          Int      @id @default(autoincrement())
+    suit        Suit
+    value       Value
+}
+
+model Suit {
+    value       Int     @id @default(autoincrement())
+    name        String
+}
+
+model Value {
+    value       Int     @id @default(autoincrement())
+    name        String
+}
```


